import os
import unittest
from typing import List, Dict

import compare_csv
import stat_utils

'''
TEST1_CSV_PATH = os.path.join('.', 'tests', 'csv', 'test1.csv')
TEST2_CSV_PATH = os.path.join('.', 'tests', 'csv', 'test2.csv')


class TestSumTransformationDurationByIteration(unittest.TestCase):
    def test(self):
        csv_data = compare_csv.get_csv_data([TEST1_CSV_PATH])
        for model_info, model_data in csv_data[0].items():
            stat_utils.get_model_sum_units_durations_by_iteration(model_data, 'transformation')
'''
